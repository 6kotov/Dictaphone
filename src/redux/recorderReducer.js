const initialState = {
  recordingDuration: null,
  isRecording: false,
  recColor: "black",
};

export const recorderReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
